name: Metals data CI

on:
  push:
    branches: [main]
    paths:
      - "website/**"
      - ".github/workflows/metals-data-ci.yml"
      - ".github/workflows/metals-data-publish.yml"
  pull_request:
    branches: [main]
    paths:
      - "website/**"
      - ".github/workflows/metals-data-ci.yml"
      - ".github/workflows/metals-data-publish.yml"

permissions:
  contents: read

concurrency:
  group: metals-data-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  data-pipeline:
    name: Check monthly data pipeline
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: website/data-pipeline

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: website/data-pipeline/package-lock.json

      - name: Install locked dependencies
        run: npm ci

      - name: Run checks
        run: npm run check

  website:
    name: Check SvelteKit API
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: website
    env:
      PUBLIC_SITE_URL: https://kara.example
      PUBLIC_APP_STORE_URL: https://apps.apple.com/app/kara/id000000000
      PUBLIC_GOOGLE_PLAY_URL: https://play.google.com/store/apps/details?id=app.kara
      PUBLIC_SUPPORT_EMAIL: support@kara.example
      PUBLIC_LEGAL_NAME: Kara

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.15.1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm
          cache-dependency-path: website/pnpm-lock.yaml

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Check types
        run: pnpm check

      - name: Run unit tests
        run: pnpm test:unit --run

      - name: Build SvelteKit server
        run: pnpm build

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Test public metals endpoints
        run: pnpm exec playwright test tests/metals-api.spec.ts --project=desktop-chromium

  workflows:
    name: Validate GitHub Actions workflows
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.x"
          cache: false

      - name: Run actionlint
        run: >-
          go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
          .github/workflows/metals-data-ci.yml
          .github/workflows/metals-data-publish.yml
